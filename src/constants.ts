export const WORLD_SIZE = 128;
export const TILE_SIZE = 32;
export const CHUNK_SIZE = 16; // tiles per chunk (16x16)
export const PLAYER_SPEED = 3; // max speed in pixels per frame
export const PLAYER_ACCELERATION = 0.5; // acceleration in pixels per frame^2
export const PLAYER_FRICTION = 0.8; // friction multiplier (lower = more friction)
export const COLLISION_PADDING = 2; // pixels from tile boundary
